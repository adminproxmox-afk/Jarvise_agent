[project]
name = "jarvis-desktop-assistant"
version = "0.1.0"
description = "Production-style Windows desktop AI assistant foundation."
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]
